# apprunner.yaml  (must be at repository root, exact name)
version: 1.0
runtime: nodejs18   # <- this exact lowercase token; not "nodejs20", not "NODEJS_18"

build:
  commands:
    - npm ci

run:
  command: npm start
  network:
    port: 3000
